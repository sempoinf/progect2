#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include<stdlib.h>
int main(void) {
	int N,f,s = 0;//число вершин
	int i, j = 0;
	int** graph;
	
	printf("Input the number of your knots: ");
	scanf("%d", &N);
	printf("%d", N);
	
	graph = (int**)malloc(N*N* sizeof(int));
	if (!graph) {
		printf("Allocation failure.");
		exit(1);
	}
	printf("Input the graph: ");
	
	
	getchar() = '-';


	
	
	
	

}
